% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_break.R
\name{lnk_pipeline_break}
\alias{lnk_pipeline_break}
\title{Segment the Stream Network at Configured Break Positions}
\usage{
lnk_pipeline_break(
  conn,
  aoi,
  cfg,
  schema,
  observations = "bcfishobs.observations"
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{aoi}{Character. Watershed group code (today; extends to other
spatial filters later).}

\item{cfg}{An \code{lnk_config} object from \code{\link[=lnk_config]{lnk_config()}}.}

\item{schema}{Character. Working schema name.}

\item{observations}{Character. Schema-qualified observations table
(default \code{"bcfishobs.observations"}).}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Fourth phase of the habitat classification pipeline. Builds the
remaining break-source tables (observations, habitat endpoints,
crossings) that depend on AOI- and config-specific data, then runs
\code{\link[fresh:frs_break_apply]{fresh::frs_break_apply()}} sequentially over the break sources in
the order defined by the config. After each round, \code{id_segment} is
reassigned so downstream rounds see contiguous integer IDs.
}
\details{
The break-source order matters. bcfishpass processes:
observations → gradient_minimal → barriers_definite →
habitat_endpoints → crossings. This order is encoded in the bundled
bcfishpass config (\code{cfg$pipeline$break_order}) and is the default
when the config does not specify one.

Writes to (under the caller's working schema unless noted):
\itemize{
\item \verb{<schema>.observations_breaks} — WSG- and species-filtered
observation positions, data-error exclusions applied
\item \verb{<schema>.habitat_endpoints} — both DRM and URM from the habitat
classification table (matches bcfishpass convention)
\item \verb{<schema>.crossings_breaks} — crossing positions
\item Mutates \code{fresh.streams} in place — adds segment boundaries at
each break source position, reassigns \code{id_segment}
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg  <- lnk_config("bcfishpass")
schema <- "working_bulk"

lnk_pipeline_setup(conn, schema)
lnk_pipeline_load(conn, "BULK", cfg, schema)
lnk_pipeline_prepare(conn, "BULK", cfg, schema)
lnk_pipeline_break(conn, "BULK", cfg, schema)

DBI::dbGetQuery(conn,
  "SELECT count(*) FROM fresh.streams")

DBI::dbDisconnect(conn)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()}
}
\concept{pipeline}
