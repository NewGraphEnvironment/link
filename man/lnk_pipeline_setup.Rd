% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_setup.R
\name{lnk_pipeline_setup}
\alias{lnk_pipeline_setup}
\title{Set Up the Working Schema for a Pipeline Run}
\usage{
lnk_pipeline_setup(conn, schema = "working", overwrite = FALSE)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object (localhost fwapg,
typically from \code{\link[=lnk_db_conn]{lnk_db_conn()}}).}

\item{schema}{Character. Working schema name for this run. Default
\code{"working"}. Validated as a SQL identifier.}

\item{overwrite}{Logical. If \code{TRUE}, drop \code{schema} (CASCADE) before
creating. Default \code{FALSE} — create only if absent so cached
contents from prior runs survive.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Creates the per-run working schema and ensures the \code{fresh} output
schema exists. Every downstream pipeline helper (\verb{lnk_pipeline_*})
assumes these schemas are in place.
}
\details{
When running multiple AOIs (watershed groups, mapsheets, sub-basins)
in parallel on the same host, each run uses its own namespaced
working schema so the runs do not collide. The caller decides the
schema name — a typical WSG-based choice is
\code{paste0("working_", tolower(aoi))}.
}
\examples{
\dontrun{
conn <- lnk_db_conn()

# Single-AOI run, canonical per-WSG schema
lnk_pipeline_setup(conn, "working_bulk")

# Fresh start: wipe any prior state first
lnk_pipeline_setup(conn, "working_bulk", overwrite = TRUE)

DBI::dbDisconnect(conn)
}
}
\concept{pipeline}
