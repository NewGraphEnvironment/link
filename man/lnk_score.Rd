% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_score.R
\name{lnk_score}
\alias{lnk_score}
\title{Score crossings}
\usage{
lnk_score(
  conn,
  crossings,
  method = c("severity", "rank"),
  thresholds = lnk_thresholds(),
  col_drop = "outlet_drop",
  col_slope = "culvert_slope",
  col_length = "culvert_length_m",
  col_severity = "severity",
  rules = NULL,
  col_id = "modelled_crossing_id",
  col_score = "priority_score",
  to = NULL,
  verbose = TRUE
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{crossings}{Character. Schema-qualified crossings table.}

\item{method}{Character. \code{"severity"} for biological impact
classification (high/moderate/low), or \code{"rank"} for weighted
multi-criteria ranking.}

\item{thresholds}{List. For \code{method = "severity"}. Output of
\code{\link[=lnk_thresholds]{lnk_thresholds()}}.}

\item{col_drop, col_slope, col_length}{Character. Column names for
\code{method = "severity"}. Defaults match PSCIS field names.}

\item{col_severity}{Character. Output column name for severity.}

\item{rules}{Named list. For \code{method = "rank"}. Each rule has
\code{col} or \code{sql}, optional \code{weight} and \code{direction}.}

\item{col_id}{Character. Primary key for \code{method = "rank"}.}

\item{col_score}{Character. Output column name for rank score.}

\item{to}{Character. If \code{NULL}, updates in-place. Otherwise copies.}

\item{verbose}{Logical. Report distribution.}
}
\value{
The table name (invisibly).
}
\description{
Classify crossings by severity or rank them by weighted criteria.
}
\examples{
\dontrun{
conn <- lnk_db_conn()

# Severity classification
lnk_score(conn, "working.crossings", method = "severity")

# Custom thresholds
lnk_score(conn, "working.crossings", method = "severity",
  thresholds = lnk_thresholds(high = list(outlet_drop = 0.8)))

# Weighted ranking
lnk_score(conn, "working.crossings", method = "rank",
  rules = list(
    habitat = list(col = "spawning_km", weight = 3),
    severity = list(sql = "CASE severity
      WHEN 'high' THEN 3 WHEN 'moderate' THEN 2 ELSE 1 END",
      weight = 2)))
}

}
