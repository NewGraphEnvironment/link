% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_inputs_verify.R
\name{lnk_inputs_verify}
\alias{lnk_inputs_verify}
\title{Verify that required Postgres tables exist in a connection}
\usage{
lnk_inputs_verify(conn, required)
}
\arguments{
\item{conn}{A DBI connection.}

\item{required}{Character vector of fully-qualified \verb{<schema>.<table>}
strings (e.g. \code{c("whse_fish.pscis_assessment_svw", "fresh.dams")}).
Identifiers are not quoted — bare lowercase form expected.}
}
\value{
\code{invisible(NULL)} on success. \code{stop()}s with a list of missing
tables on failure.
}
\description{
Fail-loud precondition check used by pipeline phases that assume their
input tables are already loaded (typically by a separate snapshot
script). Lists every missing \verb{<schema>.<table>} in the error message
so the caller knows exactly what to load before re-running.
}
\details{
Generic — not specific to any pipeline phase. Likely belongs in a
future \code{pac} package once that's scaffolded; ships in link for now.

Queries \code{information_schema.tables} once per call, parameterised with
the parsed \verb{(schema, table)} pairs — single round-trip regardless of
how many tables are in \code{required}.
}
\examples{
\dontrun{
conn <- lnk_db_conn()
lnk_inputs_verify(conn, c(
  "whse_fish.pscis_assessment_svw",
  "cabd.dams",
  "working_adms.modelled_stream_crossings"
))
}

}
\concept{inputs}
