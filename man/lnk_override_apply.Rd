% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_override_apply.R
\name{lnk_override_apply}
\alias{lnk_override_apply}
\title{Apply overrides to a crossings table}
\usage{
lnk_override_apply(
  conn,
  crossings,
  overrides,
  col_id = "modelled_crossing_id",
  cols_update = NULL,
  cols_provenance = c("reviewer", "review_date", "source"),
  verbose = TRUE
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{crossings}{Character. Schema-qualified crossings table to update
(e.g., \code{"working.crossings"}).}

\item{overrides}{Character. Schema-qualified override table (output of
\code{\link[=lnk_override_load]{lnk_override_load()}}).}

\item{col_id}{Character. Join column — the crossing identifier shared by
both tables. System-agnostic.}

\item{cols_update}{Character vector. Columns to copy from overrides to
crossings. \code{NULL} (default) auto-detects: all columns in overrides that
also exist in crossings, excluding \code{col_id} and provenance columns.}

\item{cols_provenance}{Character vector. Columns to exclude from
auto-detection (they track who reviewed, not crossing attributes).}

\item{verbose}{Logical. Report how many rows were updated.}
}
\value{
A list with \code{n_updated} (rows changed) and \code{cols_updated}
(columns that were updated), invisibly.
}
\description{
Join loaded overrides onto a crossings table and update matching columns.
Step two (or three) of the override pipeline:
\code{\link[=lnk_override_load]{lnk_override_load()}} -> \code{\link[=lnk_override_validate]{lnk_override_validate()}} -> \strong{apply}.
}
\details{
\strong{Auto-detect mode:} when \code{cols_update = NULL}, the function finds
columns that exist in both the overrides and crossings tables (excluding
the join column and provenance columns) and updates those. This means
if your override CSV has \code{barrier_result_code} and your crossings table
has \code{barrier_result_code}, it just works — no configuration needed.

\strong{Explicit mode:} set \code{cols_update = c("barrier_result_code")} when you
want precision about exactly which columns change.

\strong{Idempotent:} running twice produces the same result.
}
\examples{
# --- The override pipeline: load, then apply ---
\dontrun{
conn <- lnk_db_conn()

# Step 1: Load overrides
lnk_override_load(conn,
  csv = "data/overrides/modelled_xings_fixes.csv",
  to  = "working.overrides_modelled")

# Step 2: Apply — auto-detects which columns to update
result <- lnk_override_apply(conn,
  crossings = "working.crossings",
  overrides = "working.overrides_modelled")
# Updated 342 of 15,230 crossings (barrier_result_code)
#
# The verbose output tells you the magnitude of changes —
# essential for QA. 342 corrections from 3 years of field work.

# Step 3: Score the corrected crossings
lnk_score_severity(conn, "working.crossings")
# Severity scores now reflect field-verified barrier status,
# not just the raw modelled data.

# --- Explicit column selection ---
lnk_override_apply(conn,
  crossings   = "working.crossings",
  overrides   = "working.overrides_modelled",
  cols_update = c("barrier_result_code"))
# Only updates barrier_result_code, even if the override table
# has other columns that match.
}

}
