% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_barriers_unify.R
\name{lnk_barriers_unify}
\alias{lnk_barriers_unify}
\title{Unify per-WSG barrier sources into the working-schema \verb{<schema>.barriers}}
\usage{
lnk_barriers_unify(
  conn,
  aoi,
  cfg,
  loaded,
  schema = paste0("working_", tolower(aoi)),
  species = NULL
)
}
\arguments{
\item{conn}{A DBI connection.}

\item{aoi}{Watershed group code, e.g. \code{"PARS"}.}

\item{cfg}{An \code{lnk_config} object.}

\item{loaded}{Named list from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}. Must include
\code{loaded$parameters_fresh} (used to derive gradient-class
\code{blocks_species}).}

\item{schema}{Working schema name (per-WSG staging). Default
\code{paste0("working_", tolower(aoi))}.}

\item{species}{Character vector of species codes whose access
thresholds drive the gradient \code{blocks_species} derivation. Default
\code{unique(loaded$parameters_fresh$species_code)}. Pass a subset
(e.g. the 8 bcfp species) to control which species the gradient
blocks_species column references.}
}
\value{
\code{invisible(conn)}. Side effect: drops + recreates
\verb{<schema>.barriers}.
}
\description{
Consolidates four barrier families from the per-WSG working schema
(\verb{<schema>.crossings}, \verb{<schema>.gradient_barriers_raw}, \verb{<schema>.falls},
\verb{<schema>.barriers_subsurfaceflow}) into one \verb{<schema>.barriers}
table matching the \link{cols_barriers} shape used by the persistent
province-wide \verb{<persist_schema>.barriers}. Each row carries a
pre-computed \verb{blocks_species text[]} predicate that
\code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}} queries via \verb{WHERE 'BT' = ANY(blocks_species)}.
}
\details{
Per-WSG output is persisted to the province-wide table by
\code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}} using the same idempotent DELETE-WHERE-WSG +
INSERT pattern already used for \code{streams} and
\verb{streams_habitat_<sp>}. Cross-WSG \code{dam_dnstr_ind} resolves correctly
because \code{\link[fresh:frs_network_features]{fresh::frs_network_features()}} walks FWA topology and
doesn't care which WSG a barrier physically lives in — fixes the
PARS BT 60\% defect (PARS drains through dams in PCEA/UPCE WSGs)
and unblocks any regional run.

Source families + \code{blocks_species} semantics:
\itemize{
\item \strong{Anthropogenic} (\verb{barrier_source IN ('PSCIS','CABD','MODELLED_CROSSINGS')},
from \verb{<schema>.crossings WHERE barrier_status IN ('BARRIER','POTENTIAL')}):
blocks all 8 species. \code{crossing_source} is mapped through verbatim,
keeping the \code{MODELLED_CROSSINGS} value (vs. lossy normalization to
\code{MODELLED}).
\item \strong{Gradient} (\code{barrier_source = 'GRADIENT'}, from
\verb{<schema>.gradient_barriers_raw}): blocks species whose
\code{access_gradient_max <= gradient_class / 100}. Derived per row from
\code{loaded$parameters_fresh}.
\item \strong{Falls} (\code{barrier_source = 'FALLS'}, from \verb{<schema>.falls}):
blocks all 8 species.
\item \strong{Subsurface_flow} (\code{barrier_source = 'SUBSURFACE_FLOW'}, from
\verb{<schema>.barriers_subsurfaceflow}): blocks all 8 species. Opt-in
(only built when \code{cfg$pipeline$break_order} includes \code{"subsurfaceflow"}).
}

Remediations (PASSABLE remediation crossings) are intentionally NOT
in this table. They're consumed via \verb{<schema>.barriers_remediations}
(emitted by \code{\link[=lnk_barriers_emit]{lnk_barriers_emit()}}) for the \code{remediated_dnstr_ind}
sequence-aware logic in \code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}}, which joins to
\verb{<schema>.crossings} directly.

\code{id_barrier} is namespaced per source family so rows stay unique
inside a WSG without coordinating sequence IDs across sources.
Mirrors the offset trick \code{.lnk_crossings_union} uses for modelled
crossings.

Required pre-existing tables in \code{schema}:
\itemize{
\item \verb{<schema>.crossings} (from \code{\link[=lnk_pipeline_crossings]{lnk_pipeline_crossings()}}).
\item \verb{<schema>.gradient_barriers_raw} (from \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}}).
\item \verb{<schema>.falls} (from \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}}).
}

Optional:
\itemize{
\item \verb{<schema>.barriers_subsurfaceflow} (only when the config opts in).
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)

lnk_pipeline_setup(conn, schema = "working_pars")
lnk_pipeline_load(conn, "PARS", cfg, loaded, "working_pars")
lnk_pipeline_prepare(conn, "PARS", cfg, loaded, "working_pars",
                     conn_tunnel = conn)
lnk_pipeline_crossings(conn, "PARS", cfg, loaded, "working_pars")
lnk_barriers_unify(conn, aoi = "PARS", cfg = cfg, loaded = loaded,
                   schema = "working_pars")

DBI::dbReadTable(conn, c("working_pars", "barriers"))
}

}
\seealso{
\code{\link[=lnk_persist_init]{lnk_persist_init()}}, \code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}},
\code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}}

Other barriers: 
\code{\link{lnk_barriers_emit}()},
\code{\link{lnk_barriers_views}()}
}
\concept{barriers}
