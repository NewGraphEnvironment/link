% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_crossings.R
\name{lnk_pipeline_crossings}
\alias{lnk_pipeline_crossings}
\title{Build crossings + barriers_* tables from primitives}
\usage{
lnk_pipeline_crossings(
  conn,
  aoi,
  cfg,
  loaded,
  schema,
  snap_tolerance = 100,
  pscis_table = "whse_fish.pscis_assessment_svw",
  modelled_table = "fresh.modelled_stream_crossings",
  dams_table = paste0(schema, ".dams")
)
}
\arguments{
\item{conn}{A DBI connection.}

\item{aoi}{Watershed group code, e.g. \code{"ADMS"}.}

\item{cfg}{An \code{lnk_config} object. Currently unused; reserved for
future config-driven knobs (snap tolerance, edge-type exclusions).}

\item{loaded}{Named list from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}. Currently unused
directly (overrides already staged by \code{\link[=lnk_pipeline_load]{lnk_pipeline_load()}}); kept
in the signature for pipeline consistency.}

\item{schema}{Working schema name (e.g. \code{"working_adms"}). Must be
pre-created via \code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}}.}

\item{snap_tolerance}{Maximum PSCIS snap distance in metres. Default
\code{100} (matches bcfp).}

\item{pscis_table}{Source table for PSCIS assessments. Default
\code{"whse_fish.pscis_assessment_svw"} — the canonical BCDC view.}

\item{modelled_table}{Source table for modelled stream crossings.
Default \code{"fresh.modelled_stream_crossings"} — populated by
\code{data-raw/snapshot_bcfp.sh} (link#137). Province-wide; the AOI
filter is applied during the union.}

\item{dams_table}{Source table for CABD dams. Default
\code{paste0(schema, ".dams")} — produced per-AOI by
\code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}}.}
}
\value{
\code{invisible(conn)} for piping.
}
\description{
Composes the lean primitives-build for one AOI:
}
\details{
\enumerate{
\item \code{\link[=lnk_inputs_verify]{lnk_inputs_verify()}} — required source tables present (PSCIS, dams,
modelled_stream_crossings already loaded by \code{data-raw/snapshot_bcfp.sh}).
\item \code{\link[=lnk_points_snap]{lnk_points_snap()}} — snap PSCIS assessments to FWA via lateral KNN.
\item \code{.lnk_crossings_union()} — UNION ALL of PSCIS + CABD + modelled
sources into \verb{<schema>.crossings} (lean column set).
\item \code{.lnk_crossings_apply_overrides()} — apply user_pscis_barrier_status
\itemize{
\item user_modelled_crossing_fixes from staging tables loaded by
\code{\link[=lnk_pipeline_load]{lnk_pipeline_load()}}.
}
\item \code{\link[=lnk_barriers_emit]{lnk_barriers_emit()}} — emit \verb{<schema>.crossings_lookup} + four
\verb{<schema>.barriers_*} tables (filtered SELECTs).
}

Outputs feed \code{lnk_pipeline_access(barrier_sources = list(...))}.

Required pre-loaded tables (verified by \code{\link[=lnk_inputs_verify]{lnk_inputs_verify()}} up-front):
\itemize{
\item \code{whse_fish.pscis_assessment_svw} — BCDC PSCIS via Python \verb{bcdata bc2pg}.
\item \verb{<schema>.modelled_stream_crossings} — bchamp gpkg via curl + ogr2ogr.
\item \verb{<schema>.dams} — produced by \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}} from CABD.
}

All three are loaded by \code{data-raw/snapshot_bcfp.sh} (link#137) +
\code{lnk_pipeline_prepare()} for the \code{dams} step.

Output tables:
\itemize{
\item \verb{<schema>.crossings} — lean union (id + source + statuses + network position + geom).
\item \verb{<schema>.crossings_lookup} — slim id + statuses projection.
\item \verb{<schema>.barriers_anthropogenic}, \verb{<schema>.barriers_pscis},
\verb{<schema>.barriers_dams}, \verb{<schema>.barriers_remediations} — filtered
SELECTs ready for \code{lnk_pipeline_access(barrier_sources = list(...))}.
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg <- lnk_config("default")
loaded <- lnk_load_overrides(cfg)

lnk_pipeline_setup(conn, schema = "working_adms")
lnk_pipeline_load(conn, "ADMS", cfg, loaded, "working_adms")
lnk_pipeline_prepare(conn, "ADMS", cfg, loaded, "working_adms",
                     conn_tunnel = conn)  # cabd.dams loaded locally per #137
lnk_pipeline_crossings(conn, "ADMS", cfg, loaded, "working_adms")

# Inspect.
DBI::dbReadTable(conn, c("working_adms", "crossings_lookup"))
DBI::dbReadTable(conn, c("working_adms", "barriers_anthropogenic"))
}

}
\seealso{
\code{\link[=lnk_inputs_verify]{lnk_inputs_verify()}}, \code{\link[=lnk_points_snap]{lnk_points_snap()}}, \code{\link[=lnk_barriers_emit]{lnk_barriers_emit()}},
\code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}}

Other pipeline: 
\code{\link{lnk_pipeline_access}()},
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_mapping_code}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_run}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()},
\code{\link{lnk_presence}()}
}
\concept{pipeline}
