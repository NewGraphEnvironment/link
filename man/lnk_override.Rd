% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_override.R
\name{lnk_override}
\alias{lnk_override}
\title{Validate and apply overrides to a table}
\usage{
lnk_override(
  conn,
  crossings,
  overrides,
  col_id = "modelled_crossing_id",
  cols_update = NULL,
  cols_provenance = c("reviewer", "review_date", "reviewer_name", "source"),
  validate = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{crossings}{Character. Schema-qualified table to update.}

\item{overrides}{Character. Schema-qualified override table (output of
\code{\link[=lnk_load]{lnk_load()}}).}

\item{col_id}{Character. Join column shared by both tables.}

\item{cols_update}{Character vector. Columns to copy from overrides to
crossings. \code{NULL} (default) auto-detects: all columns in both tables
excluding \code{col_id} and \code{cols_provenance}.}

\item{cols_provenance}{Character vector. Columns to exclude from
auto-detection (provenance tracking, not data).}

\item{validate}{Logical. Run referential integrity check before
applying. Reports orphans and duplicates. Default \code{TRUE}.}

\item{verbose}{Logical. Report validation results and update counts.}
}
\value{
A list with \code{n_updated}, \code{cols_updated}, and if \code{validate = TRUE}, \code{orphans}, \code{duplicates}, \code{valid_count}, \code{total_count}.
Returned invisibly.
}
\description{
Check referential integrity (orphans, duplicates) then update matching
rows. Combines validation and application in one call.
}
\examples{
\dontrun{
conn <- lnk_db_conn()

# Load corrections
lnk_load(conn,
  csv = "data/overrides/modelled_xings_fixes.csv",
  to  = "working.fixes",
  cols_id = "modelled_crossing_id")

# Validate and apply in one step
lnk_override(conn,
  crossings = "working.crossings",
  overrides = "working.fixes")
# Override validation: working.fixes vs working.crossings
#   Total overrides:  947
#   Valid (matched):  940
#   Orphans:            7
#   Duplicates:         0
# Updated 940 of 3597 crossings (barrier_status)
}

}
