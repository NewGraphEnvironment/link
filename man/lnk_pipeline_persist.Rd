% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_persist.R
\name{lnk_pipeline_persist}
\alias{lnk_pipeline_persist}
\title{Persist per-WSG output into the province-wide habitat tables}
\usage{
lnk_pipeline_persist(
  conn,
  aoi,
  cfg,
  species,
  schema = paste0("working_", tolower(aoi))
)
}
\arguments{
\item{conn}{DBI connection.}

\item{aoi}{Watershed group code (e.g. \code{"LRDO"}).}

\item{cfg}{An \code{lnk_config} object with \code{cfg$pipeline$schema} set.}

\item{species}{Character vector of species codes to persist. Should
match what \code{lnk_persist_init()} was called with — typically
\code{\link[=lnk_pipeline_species]{lnk_pipeline_species()}} output for the AOI.}

\item{schema}{Working schema (per-WSG staging). Default
\code{paste0("working_", tolower(aoi))}.}
}
\value{
\code{conn} invisibly.
}
\description{
Copies the per-WSG staging tables (\verb{<schema>.streams},
\verb{<schema>.streams_habitat}) from the working schema into the
persistent province-wide tables (\verb{<persist_schema>.streams},
\verb{<persist_schema>.streams_habitat_<sp>}, one per species). Wide-per-
species pivot — fresh's long-format \code{streams_habitat} (one row per
segment-species) becomes one row per segment in each per-species
table.
}
\details{
Idempotent: each call DELETEs all rows for the given AOI before
INSERTing the fresh ones, so re-running a WSG cleanly replaces its
data without affecting other WSGs.

Column projection is driven by \code{cols_streams} + \code{cols_habitat} (named
vectors at the top of \code{R/lnk_persist_init.R}) — single source of
truth shared with \code{\link[=lnk_persist_init]{lnk_persist_init()}}.

Call after \code{\link[=lnk_pipeline_connect]{lnk_pipeline_connect()}} in the per-WSG orchestrator,
before computing any rollup queries that should reflect the final
per-species classification.
}
