% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_compare_wsg.R
\name{lnk_compare_wsg}
\alias{lnk_compare_wsg}
\title{Compare one watershed group against a reference dataset}
\usage{
lnk_compare_wsg(
  conn,
  aoi,
  cfg,
  loaded,
  reference = "bcfishpass",
  with_mapping_code = FALSE,
  conn_ref = NULL,
  species = NULL,
  schema = paste0("working_", tolower(aoi)),
  dams = TRUE,
  cleanup_working = TRUE
)
}
\arguments{
\item{conn}{DBI connection to the local pipeline database (typically
localhost fwapg).}

\item{aoi}{Watershed group code (e.g. \code{"ADMS"}).}

\item{cfg}{An \code{lnk_config} object (from \code{\link[=lnk_config]{lnk_config()}}).}

\item{loaded}{Named list from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}.}

\item{reference}{Character scalar identifying the reference dataset.
Currently only \code{"bcfishpass"} is supported.}

\item{with_mapping_code}{Logical. When \code{TRUE}, run the additional
\code{barriers_unify} → \code{barriers_views} → \code{access} → \code{mapping_code}
phases and emit per-species segment-match stats. Default \code{FALSE}.}

\item{conn_ref}{DBI connection to the reference database. Required
when \code{reference = "bcfishpass"} (the bcfp tunnel at
\code{localhost:63333}). Caller manages this connection.}

\item{species}{Character vector of species codes to restrict the
rollup to (e.g. \code{c("BT","CH","CM","CO","PK","SK","ST","WCT")} for
the 8 bcfp-bundle species). Default \code{NULL} uses
\code{\link[=lnk_pipeline_species]{lnk_pipeline_species()}} intersected with WSG presence.}

\item{schema}{Working schema name. Default
\code{paste0("working_", tolower(aoi))}.}

\item{dams}{Logical. When \code{TRUE} (default), pass \code{conn} as
\code{conn_tunnel} to \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}} so the CABD dams step
runs from local \code{cabd.dams}. Pass \code{FALSE} to skip dams entirely.}

\item{cleanup_working}{Logical. When \code{TRUE} (default), drop the
\verb{<schema>} working schema at the end. Pass \code{FALSE} for interactive
debug / manual inspection.}
}
\value{
A list with two elements:
\itemize{
\item \code{rollup}: tibble with one row per (species, habitat_type) — 7
habitat types: \code{spawning}, \code{rearing}, \code{lake_rearing},
\code{wetland_rearing}, \code{rearing_stream}, \code{rearing_lake_centerline},
\code{rearing_wetland_centerline}. Columns: \code{wsg}, \code{species},
\code{habitat_type}, \code{unit} (\code{km} | \code{ha}), \code{link_value},
\code{ref_value}, \code{diff_pct}.
\item \code{mapping_code}: tibble with one row per species — segment-level
match stats vs \code{bcfishpass.streams_mapping_code}. Columns:
\code{wsg}, \code{species}, \code{total_segs}, \code{match_pct}, \code{n_diffs},
\code{top_pattern}, \code{top_pattern_count}. \code{NULL} when
\code{with_mapping_code = FALSE}.
}
}
\description{
Per-WSG convenience wrapper around the existing \verb{lnk_pipeline_*}
helpers that produces a long-format rollup tibble suitable for
provincial parity comparisons. Optionally adds a per-segment
\code{mapping_code} lens when \code{with_mapping_code = TRUE}.
}
\details{
\code{reference} is a string identifying the comparison source. Today
only \code{"bcfishpass"} is supported (queries \verb{bcfishpass.habitat_linear_<sp>}
on \code{conn_ref}). The arg is future-proofed for default-bundle parity,
regression detection across link runs, or non-bcfp external data.
\subsection{Additive, not duplicative}{

Both \code{with_mapping_code = FALSE} and \code{with_mapping_code = TRUE} run
the per-WSG pipeline \strong{once}. The \code{TRUE} path is purely additive —
it adds \code{lnk_barriers_unify}, \code{lnk_barriers_views}, \code{lnk_pipeline_access},
and \code{lnk_pipeline_mapping_code} phases on top of the same network
state, then queries \code{bcfishpass.streams_mapping_code} for the segment-
level diff. The rollup tibble is unchanged between the two modes.

Side effects: writes per-WSG segment-level data to the persistent
\verb{<persist_schema>.streams} + \verb{streams_habitat_<sp>} tables via
\code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}}. Drops the \verb{<schema>} working schema at end
unless \code{cleanup_working = FALSE}.

Rollup methodology mirrors what bcfp's \verb{habitat_linear_<sp>} measures:
linear km from \code{length_metre} summed over rearing/spawning-flagged
segments, with edge-type decomposition into stream / lake-centerline /
wetland-centerline slices. Lake / wetland area in hectares uses
\verb{DISTINCT waterbody_key} joins to \code{whse_basemapping.fwa_lakes_poly} /
\code{fwa_wetlands_poly} to avoid double-counting multi-segment lakes.
See \code{research/default_vs_bcfishpass.md} for the measurement-asymmetry
decision (link reports both centerline km and polygon ha; bcfp credits
only one depending on species rule).
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
conn_ref <- DBI::dbConnect(RPostgres::Postgres(),
  host = "localhost", port = 63333, dbname = "bcfishpass",
  user = "newgraph", password = Sys.getenv("PG_PASS_SHARE"))
cfg <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)

# Rollup-only (~70s per WSG)
result <- lnk_compare_wsg(
  conn = conn, aoi = "ADMS",
  cfg = cfg, loaded = loaded,
  reference = "bcfishpass", conn_ref = conn_ref
)
print(result$rollup)

# Add mapping_code lens (~100s per WSG)
result_mc <- lnk_compare_wsg(
  conn = conn, aoi = "ADMS",
  cfg = cfg, loaded = loaded,
  reference = "bcfishpass", conn_ref = conn_ref,
  with_mapping_code = TRUE
)
print(result_mc$mapping_code)
}

}
\seealso{
\code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}}, \code{\link[=lnk_pipeline_load]{lnk_pipeline_load()}},
\code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}}, \code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}},
\code{\link[=lnk_parity_annotate]{lnk_parity_annotate()}}

Other compare: 
\code{\link{lnk_compare_rollup}()},
\code{\link{lnk_parity_annotate}()}
}
\concept{compare}
