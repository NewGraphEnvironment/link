% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_access.R
\name{lnk_pipeline_access}
\alias{lnk_pipeline_access}
\title{Build per-segment access codes + downstream-feature arrays}
\usage{
lnk_pipeline_access(
  conn,
  segments,
  aoi,
  to = NULL,
  barriers_per_sp = list(),
  observations = NULL,
  wsg_presence = list(),
  barrier_sources = list(),
  segment_id_col = "id_segment"
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object pointing at fwapg.}

\item{segments}{Character. Schema-qualified segments table.}

\item{aoi}{Character. Watershed group code (e.g. \code{"ADMS"}). Filter
applied to segments via \code{watershed_group_code = aoi}.}

\item{to}{Character or \code{NULL}. Optional schema-qualified output
table. When supplied, the wide \code{streams_access} shape is written
via \code{dbWriteTable(overwrite = TRUE)}; in either case the tibble
is returned invisibly. Default \code{NULL} returns-only.}

\item{barriers_per_sp}{Named list. Each name is a species code (e.g.
\code{"bt"}); each value is a schema-qualified barriers table for that
species (e.g. \code{"working_adms.barriers_bt"}). Each barriers table
must have a \verb{<sp>_id} column (e.g. \code{barriers_bt_id}) plus the
standard \verb{(blue_line_key, downstream_route_measure, wscode_ltree, localcode_ltree)} keys.}

\item{observations}{Character or \code{NULL}. Optional schema-qualified
observations table with \verb{(observation_key, species_code, ...)} +
the standard FWA keys. When provided, drives the access code
distinction between \code{1} (modelled) and \code{2} (observed). Default
\code{NULL} collapses observation-distinguishing logic — every
accessible segment gets \verb{access_<sp> = 1}.}

\item{wsg_presence}{Named logical. One per species (matching
\code{barriers_per_sp} keys), \code{TRUE} when the species is present in
\code{aoi}. Sets \verb{access_<sp> = -9} for species marked \code{FALSE}. Default
empty list assumes all species present (no -9 codes emitted).}

\item{barrier_sources}{Named list. Each name is an arbitrary source
tag (e.g. \code{"anthropogenic"}, \code{"pscis"}, \code{"dams"}); each value is a
schema-qualified barriers table for that source. Output gains one
\verb{has_barriers_<source>_dnstr} boolean column per source. Unlike
\code{barriers_per_sp}, sources here don't drive the species access
integer code -- they're the bcfp-shape dnstr indicators consumed
by \code{lnk_pipeline_mapping_code}. Optional; default empty.}

\item{segment_id_col}{Character. Default \code{"id_segment"}.}
}
\value{
\code{conn} invisibly, for piping.
}
\description{
Composes \code{\link[fresh:frs_network_features]{fresh::frs_network_features()}} calls across species (and
optionally observations) to produce a \code{streams_access} wide table
that mirrors \code{bcfishpass.streams_access}'s shape — one row per
segment, with per-species \verb{barriers_<sp>_dnstr} arrays and per-
species integer \verb{access_<sp>} codes derived via CASE on
(wsg-presence × dnstr-empty × observed-upstream).
}
\details{
This phase runs after \code{\link[=lnk_pipeline_classify]{lnk_pipeline_classify()}} returns and before
\code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}} cleanup_working drops the working schema —
it needs the per-species \verb{barriers_<sp>} tables that prepare/break
built. Output goes to \verb{<schema>.streams_access} (working schema,
picked up by persist on the next commit).

Access integer codes per species (mirroring bcfp):
-9 = species not present in WSG (per \code{wsg_presence})
0 = barriers downstream (blocked)
1 = no barriers downstream + species not observed upstream
(modelled accessible)
2 = no barriers downstream + species observed upstream
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_mapping_code}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()}
}
\concept{pipeline}
