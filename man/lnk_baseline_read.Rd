% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_baseline_read.R
\name{lnk_baseline_read}
\alias{lnk_baseline_read}
\title{Read the run-tracking baseline ledger}
\usage{
lnk_baseline_read(path = "data-raw/logs/bcfp_baselines.csv")
}
\arguments{
\item{path}{Path to the ledger CSV. Defaults to
\code{data-raw/logs/bcfp_baselines.csv} relative to the working directory.}
}
\value{
A tibble with one row per recorded run. Columns:
\code{run_started_pdt}, \code{host}, \code{run_label}, \code{link_schema},
\code{bcfp_model_run_id}, \code{bcfp_model_version}, \code{bcfp_date_completed},
\code{notes}. All character. \code{bcfp_model_run_id} may be empty for
workflow-generated rows that lacked DB-tunnel access (Path 2).
}
\description{
Loads the per-run baseline CSV (each row stamps which upstream build a
particular comparison or sync ran against) into a tibble and validates
the column shape matches \code{cols_baseline}.
}
\details{
Companion: \code{\link[=lnk_baseline_append]{lnk_baseline_append()}} writes rows; this reads them.

Fails loud if the file's column header doesn't match \code{cols_baseline}.
Schema migrations to the ledger should update \code{cols_baseline} in
\code{R/lnk_baseline_read.R} and migrate the CSV in lockstep.
}
\examples{
\dontrun{
baseline <- lnk_baseline_read()
tail(baseline)

# Filter to csv-sync-generated rows.
subset(baseline, grepl("^csv-sync-", run_label))
}

}
\seealso{
\code{\link[=lnk_baseline_append]{lnk_baseline_append()}}

Other baseline: 
\code{\link{lnk_baseline_append}()}
}
\concept{baseline}
