% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_compare_rollup.R
\name{lnk_compare_rollup}
\alias{lnk_compare_rollup}
\title{Compare one watershed group's persisted state against a reference}
\usage{
lnk_compare_rollup(
  conn,
  aoi,
  cfg,
  reference = "bcfishpass",
  conn_ref = NULL,
  species = NULL
)
}
\arguments{
\item{conn}{DBI connection to the local pipeline database (where
\verb{<persist_schema>} lives).}

\item{aoi}{Watershed group code (e.g. \code{"ADMS"}).}

\item{cfg}{An \code{lnk_config} object (used only to resolve
\code{cfg$pipeline$schema} for the persisted table names).}

\item{reference}{Character scalar identifying the reference dataset.
Currently only \code{"bcfishpass"} is supported.}

\item{conn_ref}{DBI connection to the reference database. Required
when \code{reference = "bcfishpass"} (bcfp tunnel at
\code{localhost:63333}).}

\item{species}{Optional character vector of species codes
(e.g. \code{c("BT","CO")}) to restrict the rollup to. Default \code{NULL}
discovers the set from PG.}
}
\value{
A tibble with one row per (species, habitat_type) — 7
habitat types per species. Columns: \code{wsg}, \code{species},
\code{habitat_type}, \code{unit} (\code{km} | \code{ha}), \code{link_value}, \code{ref_value},
\code{diff_pct}.
}
\description{
Comparison-only counterpart to \code{\link[=lnk_pipeline_run]{lnk_pipeline_run()}}. Reads the
persisted \verb{<persist_schema>.streams} + \verb{streams_habitat_<sp>} tables
that \code{lnk_pipeline_run()} wrote, queries the reference dataset, and
returns a long-format diff tibble.
}
\details{
Reads only — no writes to PG, no working schema. Caller persists the
return value (e.g. \code{saveRDS}) if a side-artifact is wanted; that's a
separate decision from whether the model itself ran.

\code{reference} is a string identifying the comparison source. Today
only \code{"bcfishpass"} is supported (queries \verb{bcfishpass.habitat_linear_<sp>}
on \code{conn_ref}). The arg is future-proofed for default-bundle parity,
regression detection across link runs, or non-bcfp external data —
new references plug in without renaming the public arg.
\subsection{Species resolution}{

If \code{species = NULL} (default), the active species set is discovered
from PG: any \verb{<persist_schema>.streams_habitat_<sp>} table with rows
for the requested WSG. This means the rollup is grounded in actual
persisted state — no need for \code{cfg$species} or
\code{wsg_species_presence} lookups here.

If \code{species} is passed explicitly, it's intersected with the
PG-discovered set (caller-passed species absent from PG simply drop
out — no error).
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
conn_ref <- DBI::dbConnect(RPostgres::Postgres(),
  host = "localhost", port = 63333, dbname = "bcfishpass",
  user = "newgraph", password = Sys.getenv("PG_PASS_SHARE"))
cfg <- lnk_config("bcfishpass")

# Compare-only against existing PG state (~2s).
rollup <- lnk_compare_rollup(
  conn = conn, aoi = "ADMS", cfg = cfg,
  reference = "bcfishpass", conn_ref = conn_ref
)
print(rollup)
}

}
\seealso{
\code{\link[=lnk_pipeline_run]{lnk_pipeline_run()}}, \code{\link[=lnk_compare_wsg]{lnk_compare_wsg()}},
\code{\link[=lnk_parity_annotate]{lnk_parity_annotate()}}

Other compare: 
\code{\link{lnk_compare_wsg}()},
\code{\link{lnk_mapping_code}()},
\code{\link{lnk_parity_annotate}()}
}
\concept{compare}
