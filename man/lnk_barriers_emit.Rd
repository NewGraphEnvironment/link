% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_barriers_emit.R
\name{lnk_barriers_emit}
\alias{lnk_barriers_emit}
\title{Emit slim crossings_lookup + per-source barriers_* tables}
\usage{
lnk_barriers_emit(conn, schema)
}
\arguments{
\item{conn}{A DBI connection.}

\item{schema}{Working schema name (already-existing). Must contain
\verb{<schema>.crossings}. Optionally contains \verb{<schema>.dams}; if absent,
\code{barriers_dams} is created empty.}
}
\value{
\code{invisible(NULL)}. Side effect: drops + recreates the five
tables in \code{schema}.
}
\description{
Given a \verb{<schema>.crossings} table (bcfp-shaped — produced upstream by
\code{\link[=lnk_pipeline_crossings]{lnk_pipeline_crossings()}} or by loading a bcfp-format external dump)
plus optional \verb{<schema>.dams}, emits five derived tables via filtered
SELECTs:
}
\details{
\itemize{
\item \verb{<schema>.crossings_lookup} (slim id + status projection)
\item \verb{<schema>.barriers_anthropogenic} (all barrier-status crossings)
\item \verb{<schema>.barriers_pscis} (PSCIS-sourced barrier-status crossings)
\item \verb{<schema>.barriers_dams} (dam-sourced barrier-status crossings)
\item \verb{<schema>.barriers_remediations} (anthropogenic UNION REMEDIATED-PASSABLE)
}

Output column shapes match what \code{lnk_pipeline_access(barrier_sources = list(...))}
consumes — \code{aggregated_crossings_id} plus the network-position columns
(\code{linear_feature_id}, \code{blue_line_key}, \code{downstream_route_measure},
\code{wscode_ltree}, \code{localcode_ltree}).

Mostly bcfp-shape-specific — it relies on column names/values from
bcfp's \code{crossings} shape (\code{barrier_status}, \code{crossing_source},
\code{pscis_status}). Lives in link as the emit step of the new
\code{lnk_pipeline_crossings()} phase; may move to a future \code{pac} package
once that's scaffolded.

Filters mirror bcfp's \verb{model/01_access/sql/barriers_*.sql} and
\code{remediations_barriers.sql}:
\itemize{
\item \verb{barrier_status IN ('BARRIER', 'POTENTIAL')} for anthropogenic-style tables.
\item \code{blue_line_key = watershed_key} (excludes side-channel features).
\item \code{barriers_remediations} = \code{barriers_anthropogenic} UNION
crossings WHERE \verb{pscis_status = 'REMEDIATED' AND barrier_status = 'PASSABLE'}
(bcfp-intended logic per the v0.30.2 fix; see \code{smnorris/bcfishpass#891}).
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
lnk_pipeline_setup(conn, schema = "working_adms")
# ... lnk_pipeline_crossings(...) populates working_adms.crossings ...
lnk_barriers_emit(conn, schema = "working_adms")
DBI::dbReadTable(conn, c("working_adms", "crossings_lookup"))
}

}
\seealso{
Other barriers: 
\code{\link{lnk_barriers_unify}()},
\code{\link{lnk_barriers_views}()}
}
\concept{barriers}
