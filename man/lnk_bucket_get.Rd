% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_bucket_get.R
\name{lnk_bucket_get}
\alias{lnk_bucket_get}
\title{Download a single artifact from a public S3 bucket prefix}
\usage{
lnk_bucket_get(
  name,
  prefix = "https://fresh-bc.s3.us-west-2.amazonaws.com/bcfishpass",
  to = NULL
)
}
\arguments{
\item{name}{File path relative to \code{prefix}, e.g. \code{"log.json"} or
\code{"csvs/wsg_species_presence.csv"}.}

\item{prefix}{Bucket prefix as an HTTPS URL. Defaults to NGE's bcfp dump
prefix.}

\item{to}{Optional file path. When supplied, bytes are written there
(binary) and the path is returned invisibly. When \code{NULL} (default),
raw bytes are returned in memory.}
}
\value{
Either a \code{raw} vector (default) or, when \code{to} is supplied, the
path it was written to (invisibly).
}
\description{
Fetch one file from a versioned S3 bucket (e.g., the bcfp build artifacts
under \verb{s3://fresh-bc/bcfishpass/}). Returns raw bytes by default so
callers can decode based on file format (CSV via \code{read.csv}, JSON via
\code{jsonlite::fromJSON}, parquet via \code{arrow::read_parquet}, etc.) — the
helper is deliberately format-agnostic.
}
\details{
Companion function: \code{\link[=lnk_bucket_log]{lnk_bucket_log()}} is sugar for the most common
read (\verb{<prefix>/log.json}, the bcfp build identifier).

Uses \code{httr::GET()}. Fails loud (\code{stop()}) on any non-2xx response with
the URL + status code in the message. No retry/back-off — re-running
the workflow is the recovery path.

Public bucket — no AWS auth needed for read. Writes happen via the
upstream \code{dump-bcfishpass-csvs.yml} workflow (separate; not this
function).
}
\examples{
\dontrun{
# Read the build identifier directly via the sugar helper.
log <- lnk_bucket_log()
log$model_version

# Or fetch the same file as raw bytes and decode yourself.
bytes <- lnk_bucket_get("log.json")
jsonlite::fromJSON(rawToChar(bytes))

# Pull a CSV and parse with read.csv (no temp file needed).
bytes <- lnk_bucket_get("csvs/wsg_species_presence.csv")
df <- read.csv(text = rawToChar(bytes))
head(df)

# Stream a large file straight to disk.
tmp <- tempfile(fileext = ".csv")
lnk_bucket_get("csvs/user_modelled_crossing_fixes.csv", to = tmp)
file.info(tmp)$size
}

}
\seealso{
\code{\link[=lnk_bucket_log]{lnk_bucket_log()}}

Other bucket: 
\code{\link{lnk_bucket_log}()}
}
\concept{bucket}
