% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_parity_annotate.R
\name{lnk_parity_annotate}
\alias{lnk_parity_annotate}
\title{Annotate a parity rollup against the bcfp divergence taxonomy}
\usage{
lnk_parity_annotate(rollup, taxonomy, to = NULL, tolerance = 2)
}
\arguments{
\item{rollup}{A tibble with columns \code{wsg}, \code{species}, \code{habitat_type},
\code{link_value}, \code{diff_pct}, plus one of \code{ref_value} (library shape)
or \code{bcfishpass_value} (data-raw wrapper shape). Both shapes pass
through — the function normalizes internally.}

\item{taxonomy}{Path to a YAML file or a parsed list (from
\code{\link[yaml:read_yaml]{yaml::read_yaml()}}). When a path, the function reads it. When a
parsed list, it must have an \code{entries} element holding the
per-pattern records.}

\item{to}{Optional character path. When set, writes the annotated
tibble to a CSV and returns it invisibly.}

\item{tolerance}{Numeric. Rows with \verb{|diff_pct| < tolerance} and no
taxonomy match are tagged \code{WITHIN_TOLERANCE} instead of
\code{UNEXPLAINED}. Default \code{2} (matching the acceptance bar in #162).}
}
\value{
A tibble extending \code{rollup} with annotation columns
\code{taxonomy_id}, \code{class}, \code{mechanism}, \code{status}, \code{refs}. \code{refs} is a
semicolon-collapsed string for CSV-friendliness.
}
\description{
Joins each row of a parity rollup (from \code{\link[=lnk_compare_wsg]{lnk_compare_wsg()}} or the
\code{data-raw/compare_bcfishpass_wsg.R} wrapper) to the first taxonomy
entry whose \code{wsg}, \code{species}, \code{metric}, \code{pattern}, and (optional)
\code{diff_range} all match. Unmatched rows with \verb{|diff_pct| >= tolerance} are tagged \verb{class = UNEXPLAINED, status = NEEDS_INVESTIGATION}; smaller residuals become \verb{class = WITHIN_TOLERANCE, status = CLOSED}. Rows with \code{NA} diff_pct (NA ref
value, divide-by-zero) become \code{class = NOT_APPLICABLE}.
}
\details{
First-match-wins: taxonomy entries are evaluated in the order they
appear in the YAML file. Put the most specific entries first.
}
\examples{
\dontrun{
rollup <- readRDS("data-raw/logs/provincial_parity/ADMS.rds")
annotated <- lnk_parity_annotate(
  rollup,
  taxonomy = "research/bcfp_divergence_taxonomy.yml",
  to = "data-raw/logs/provincial_parity/ADMS_annotated.csv"
)

# Acceptance check
unexplained <- annotated[annotated$class == "UNEXPLAINED" &
                          abs(annotated$diff_pct) >= 2, ]
stopifnot(nrow(unexplained) == 0L)
}

}
\seealso{
\code{\link[=lnk_compare_wsg]{lnk_compare_wsg()}}

Other compare: 
\code{\link{lnk_access}()},
\code{\link{lnk_compare_mapping_code}()},
\code{\link{lnk_compare_rollup}()},
\code{\link{lnk_compare_wsg}()},
\code{\link{lnk_mapping_code}()},
\code{\link{lnk_rollup_wsg}()}
}
\concept{compare}
