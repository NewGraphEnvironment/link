% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_mapping_code.R
\name{lnk_pipeline_mapping_code}
\alias{lnk_pipeline_mapping_code}
\title{Build per-segment per-species mapping_code strings (bcfp parity)}
\usage{
lnk_pipeline_mapping_code(
  access,
  habitat,
  feature_code,
  to = NULL,
  conn = NULL,
  presence = NULL,
  resident_species = c("bt", "wct"),
  anadromous_species = c("ch", "cm", "co", "pk", "sk", "st"),
  spawn_only_species = c("cm", "pk"),
  segment_id_col = "id_segment",
  intermittent_feature_code = "GA24850150"
)
}
\arguments{
\item{access}{A tibble or data.frame keyed by \code{segment_id_col} with
\verb{has_barriers_<sp>_dnstr} boolean per species, plus the bcfp-shape
sources \code{has_barriers_anthropogenic_dnstr},
\code{has_barriers_pscis_dnstr}, \code{has_barriers_dams_dnstr}, and (optional)
\code{has_remediated_dnstr}. Typically the output of
\code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}} called with \code{barrier_sources} populated.}

\item{habitat}{A tibble keyed by \code{segment_id_col} with
\verb{spawning_<sp>} and \verb{rearing_<sp>} numeric columns per species.
Mirrors bcfp's \code{streams_habitat_linear} shape.}

\item{feature_code}{Named character or data.frame. Either a named
character vector mapping \code{segment_id} -> \code{feature_code}, or a
data.frame with \code{segment_id_col} and \code{"feature_code"} columns.
Used for the \code{INTERMITTENT} flag.}

\item{to}{Character or \code{NULL}. Optional schema-qualified destination
table. When supplied, the result tibble is written via
\code{dbWriteTable(overwrite = TRUE)} and the tibble is also returned
(so callers can chain into \code{lnk_pipeline_persist} / \code{build_species_views.R}).
Default \code{NULL} returns-only.}

\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}. Required only when \code{to} is
supplied; ignored otherwise.}

\item{resident_species}{Character. Species using the resident flavor
of \code{mapping_code_barrier}. Default \code{c("bt", "wct")}.}

\item{anadromous_species}{Character. Species using the anadromous
flavor. Default \code{c("ch", "cm", "co", "pk", "sk", "st")}.}

\item{spawn_only_species}{Character. Species without rearing
semantics (token 1 only emits SPAWN, never REAR). Default
\code{c("cm", "pk")}. Mirrors bcfp.}

\item{segment_id_col}{Character. Default \code{"id_segment"}.}

\item{intermittent_feature_code}{Character. The \code{feature_code} value
that flags an intermittent stream. Default \code{"GA24850150"} (bcfp).}
}
\value{
A tibble keyed by \code{segment_id_col} with one
\verb{mapping_code_<sp>} character column per species in
\code{union(resident_species, anadromous_species)}.
}
\description{
Mirrors \code{bcfishpass.streams_mapping_code} -- a per-segment per-species
semicolon-token compound describing the segment's habitat label, the
most-relevant downstream barrier source, and an intermittent flag if
applicable. Pure derivation over the bcfp-shape inputs (no SQL).
}
\details{
Vocabulary (per species):

\preformatted{
{ACCESS|SPAWN|REAR|""} ; {NONE|DAM|MODELLED|ASSESSED|REMEDIATED} [;INTERMITTENT]
}

Token 1 (habitat) per bcfp:
\itemize{
\item \code{ACCESS}: species' barriers all upstream (i.e. accessible to
species) AND segment has no spawning AND no rearing eligibility
for the species
\item \code{SPAWN}: spawning eligibility > 0 (always wins over REAR)
\item \code{REAR}: rearing eligibility > 0 AND no spawning
\item \code{""} (empty): species has at least one downstream barrier blocking
it (so habitat label is suppressed -- inaccessible).
\item For species without rearing semantics (CM, PK), the rearing
conditions drop out -- only \code{ACCESS} (no spawn, no barriers) and
\code{SPAWN} (spawning > 0) emit.
}

Token 2 (barrier source) only emits when the species' barriers
downstream is empty (i.e. accessible). Resident-flavor (BT, WCT) and
anadromous-flavor (CH/CM/CO/PK/SK/ST) differ in their CASE order:
\itemize{
\item Resident:    REMEDIATED > DAM > ASSESSED (anthropogenic + pscis +
no dam) > MODELLED (anthropogenic + no pscis + no
dam) > NONE (no anthropogenic).
\item Anadromous:  REMEDIATED > DAM (any dam) > ASSESSED (any pscis) >
MODELLED (any anthropogenic) > NONE.
}

Token 3 emits \code{INTERMITTENT} when the segment's \code{feature_code} matches
the bcfp intermittent code (default \code{"GA24850150"}) AND the species'
barriers downstream is empty.

Empty / NULL tokens are dropped via \code{paste(..., collapse = ";")}-style
composition so an inaccessible segment yields \code{""} (empty string) and
an accessible no-habitat-no-intermittent segment yields \code{"ACCESS;NONE"}.
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_access}()},
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_crossings}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_run}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()},
\code{\link{lnk_presence}()}
}
\concept{pipeline}
