% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_habitat_setup_schema.R
\name{lnk_habitat_setup_schema}
\alias{lnk_habitat_setup_schema}
\title{Set Up the Working Schema for a Habitat Pipeline Run}
\usage{
lnk_habitat_setup_schema(conn, schema = "working", overwrite = FALSE)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object (localhost fwapg,
typically from \code{\link[=lnk_db_conn]{lnk_db_conn()}}).}

\item{schema}{Character. Working schema name for this run. Default
\code{"working"}. Validated as a SQL identifier.}

\item{overwrite}{Logical. If \code{TRUE}, drop \code{schema} (CASCADE) before
creating. Default \code{FALSE} — create only if absent so cached
contents from prior runs survive.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Creates the per-run working schema and ensures the \code{fresh} output
schema exists. Every downstream pipeline helper (\verb{lnk_habitat_*})
assumes these schemas are in place.
}
\details{
When running multiple watershed groups in parallel on the same host,
each run uses its own namespaced working schema (e.g.
\code{working_bulk}, \code{working_adms}) so the runs do not collide. The
canonical \verb{_targets.R} call is
\code{lnk_habitat_setup_schema(conn, paste0("working_", tolower(wsg)))}.
}
\examples{
\dontrun{
conn <- lnk_db_conn()

# Single-WSG run, canonical per-WSG schema
lnk_habitat_setup_schema(conn, "working_bulk")

# Fresh start: wipe any prior state first
lnk_habitat_setup_schema(conn, "working_bulk", overwrite = TRUE)

DBI::dbDisconnect(conn)
}
}
\concept{habitat pipeline}
