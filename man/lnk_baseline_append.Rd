% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_baseline_append.R
\name{lnk_baseline_append}
\alias{lnk_baseline_append}
\title{Append a row to the run-tracking baseline ledger}
\usage{
lnk_baseline_append(
  log,
  run_label,
  link_schema = "n/a",
  notes = "",
  path = "data-raw/logs/bcfp_baselines.csv"
)
}
\arguments{
\item{log}{A list with at minimum \code{model_version} and \code{date_completed}.
Optional \code{head_sha} (full or short). The shape returned by
\code{\link[=lnk_bucket_log]{lnk_bucket_log()}} qualifies; hand-built lists also work.}

\item{run_label}{A string identifying the run, e.g.
\code{"csv-sync-20260507"}, \code{"provincial_default_extrabreaks"}.}

\item{link_schema}{The persistent target schema for the run, when
applicable. Defaults to \code{"n/a"} for runs that don't write a
pipeline schema (csv-sync, etc.).}

\item{notes}{Free-form notes column. Useful for short-sha references
or any per-run context worth recording.}

\item{path}{Path to the ledger CSV. Defaults to
\code{data-raw/logs/bcfp_baselines.csv}. Created with the canonical
header if it does not yet exist.}
}
\value{
The path the row was appended to, invisibly.
}
\description{
Records that a particular run (csv-sync, parity comparison, etc.) ran
against a specific upstream build. Constructs row from the
\code{\link[=lnk_bucket_log]{lnk_bucket_log()}} result + caller-supplied \code{run_label} / \code{notes}.
Stamps \code{run_started_pdt} (Pacific) and \code{host} (\code{Sys.info()[["nodename"]]})
automatically.
}
\details{
Validates ledger column shape on append: fails loud if the CSV header
doesn't match the expected \code{cols_baseline} shape (drift in the ledger
file is signaled, not silently corrupted).

\code{bcfp_model_run_id} is populated from \code{log$model_run_id} if present,
otherwise empty. The Path-2 (no DB tunnel) workflow doesn't have access
to \code{bcfishpass.log.model_run_id}; the SHA in \code{log$model_version} /
\code{log$head_sha} still uniquely identifies the upstream build.
}
\examples{
\dontrun{
log <- lnk_bucket_log()
tmp <- withr::local_tempfile(fileext = ".csv")
lnk_baseline_append(log,
                    run_label = "csv-sync-20260507",
                    notes = paste0("auto-append; head_sha=",
                                   substr(log$head_sha, 1, 7)),
                    path = tmp)
lnk_baseline_read(tmp)
}

}
\seealso{
\code{\link[=lnk_baseline_read]{lnk_baseline_read()}}, \code{\link[=lnk_bucket_log]{lnk_bucket_log()}}

Other baseline: 
\code{\link{lnk_baseline_current}()},
\code{\link{lnk_baseline_read}()}
}
\concept{baseline}
