% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_rules_build.R
\name{lnk_rules_build}
\alias{lnk_rules_build}
\title{Build habitat eligibility rules YAML from dimensions CSV}
\usage{
lnk_rules_build(
  csv,
  to,
  thresholds = system.file("extdata", "parameters_habitat_thresholds.csv", package =
    "fresh"),
  edge_types = c("categories", "explicit")
)
}
\arguments{
\item{csv}{Path to a dimensions CSV with columns: \code{species},
\code{spawn_lake}, \code{spawn_stream}, \code{rear_lake}, \code{rear_lake_only},
\code{rear_no_fw}, \code{rear_stream}, \code{rear_wetland}. Optional columns:
\code{river_skip_cw_min} (yes/no — skip channel_width_min on river
polygon segments), \code{notes}.}

\item{to}{Path to write the output YAML.}

\item{thresholds}{Path to the habitat thresholds CSV (from fresh).
Used to look up \code{rear_lake_ha_min} per species. Default uses the
copy shipped with fresh.}

\item{edge_types}{Character. How to express stream edge types in rules:
\code{"categories"} (default) uses fresh categories (\code{stream}, \code{canal}).
\code{"explicit"} uses integer FWA edge_type codes (\verb{1000, 1100, 2000, 2300}).}
}
\value{
Invisible path to the written YAML file.
}
\description{
Transforms a species habitat dimensions CSV into the rules YAML format
consumed by \code{\link[fresh:frs_habitat]{fresh::frs_habitat()}}. The CSV is the human-edited source
of truth; the YAML is the derived artifact.
}
\examples{
\dontrun{
# NGE defaults
lnk_rules_build(
  csv = system.file("extdata", "parameters_habitat_dimensions.csv", package = "link"),
  to = "inst/extdata/parameters_habitat_rules.yaml"
)

# bcfishpass comparison variant
lnk_rules_build(
  csv = system.file("extdata", "configs", "bcfishpass", "dimensions.csv",
                    package = "link"),
  to = "inst/extdata/configs/bcfishpass/rules.yaml",
  edge_types = "explicit"
)
}

}
