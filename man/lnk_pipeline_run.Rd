% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_run.R
\name{lnk_pipeline_run}
\alias{lnk_pipeline_run}
\title{Run the link pipeline end-to-end for one watershed group}
\usage{
lnk_pipeline_run(
  conn,
  aoi,
  cfg,
  loaded,
  schema = paste0("working_", tolower(aoi)),
  dams = TRUE,
  cleanup_working = TRUE
)
}
\arguments{
\item{conn}{DBI connection to the local pipeline database (typically
localhost fwapg).}

\item{aoi}{Watershed group code (e.g. \code{"ADMS"}). Validated against
\verb{^[A-Z]\{3,5\}$}.}

\item{cfg}{An \code{lnk_config} object (from \code{\link[=lnk_config]{lnk_config()}}).}

\item{loaded}{Named list from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}.}

\item{schema}{Working schema name. Default
\code{paste0("working_", tolower(aoi))}. Per-WSG staging tables live
here; dropped on exit when \code{cleanup_working = TRUE}.}

\item{dams}{Logical. When \code{TRUE} (default), pass \code{conn} as
\code{conn_tunnel} to \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}} so the CABD dams step
runs from local \code{cabd.dams}. Pass \code{FALSE} to skip dams entirely.}

\item{cleanup_working}{Logical. When \code{TRUE} (default), drop the
\verb{<schema>} working schema at the end. Pass \code{FALSE} for interactive
debug / manual inspection.}
}
\value{
\code{conn}, invisibly. Side effects are the writes into
\verb{<persist_schema>.streams}, \verb{streams_habitat_<sp>}, and \code{barriers}.
}
\description{
Modelling-only umbrella: chains the \verb{lnk_pipeline_*} phases and the
persist write-out into a single call. Produces per-WSG segment data
in the persistent province-wide tables (\verb{<persist_schema>.streams},
\verb{streams_habitat_<sp>} per species, \code{barriers}).
}
\details{
This is the \strong{modelling boundary} — the link package's deliverable.
Comparison against bcfishpass (or any future reference) lives in
\code{\link[=lnk_compare_rollup]{lnk_compare_rollup()}}, which reads the persisted state. The split
lets re-running the pipeline and re-running the comparison happen
independently; an orchestrator loop's resume check can probe PG
state via \code{link:::.lnk_wsg_persisted()} rather than the comparison
RDS artifact.
\subsection{Phase order}{
\enumerate{
\item \code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}} — create per-WSG working schema.
\item \code{\link[=lnk_pipeline_load]{lnk_pipeline_load()}} — crossings + modelled fixes + PSCIS status.
\item \code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}} — falls, definite + control, habitat
confirms, gradient barriers, natural barriers, barrier overrides,
per-model minimal reduction, base segments. Passes \code{conn} as
\code{conn_tunnel} when \code{dams = TRUE} so CABD dams flow through.
\item \code{\link[=lnk_pipeline_crossings]{lnk_pipeline_crossings()}} — match PSCIS to modelled crossings.
\item \code{\link[=lnk_pipeline_break]{lnk_pipeline_break()}} — observations, gradient minimal, definite,
habitat endpoints, crossings — in config-defined order.
\item \code{\link[=lnk_pipeline_classify]{lnk_pipeline_classify()}} — assemble \code{streams_breaks} and run
\code{frs_habitat_classify()}.
\item \code{\link[=lnk_pipeline_connect]{lnk_pipeline_connect()}} — per-species cluster + connected_waterbody.
\item \code{\link[=lnk_pipeline_species]{lnk_pipeline_species()}} — resolve the active species set for this
AOI (cfg$species ∩ wsg_species_presence). Empty set is an error.
\item \code{\link[=lnk_persist_init]{lnk_persist_init()}} — create persistent target tables if absent.
\item \code{\link[=lnk_barriers_unify]{lnk_barriers_unify()}} — unify per-source barriers into a single
working-schema table (always; promotes the mapping_code-only
flag in \code{lnk_compare_wsg()} to canonical PG state).
\item \code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}} — copy per-WSG streams + per-species
habitat + barriers into \verb{<persist_schema>} (idempotent
DELETE-WHERE-WSG + INSERT).
}
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)

# Model one WSG end-to-end (~70s)
lnk_pipeline_run(conn = conn, aoi = "ADMS",
                 cfg = cfg, loaded = loaded)

# Verify PG state
DBI::dbGetQuery(conn,
  "SELECT count(*) FROM fresh.streams WHERE watershed_group_code = 'ADMS'")
}

}
\seealso{
\code{\link[=lnk_compare_rollup]{lnk_compare_rollup()}}, \code{\link[=lnk_compare_wsg]{lnk_compare_wsg()}},
\code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}}, \code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}}

Other pipeline: 
\code{\link{lnk_pipeline_access}()},
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_crossings}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_mapping_code}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()},
\code{\link{lnk_presence}()}
}
\concept{pipeline}
