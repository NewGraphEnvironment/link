% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_presence.R
\name{lnk_presence}
\alias{lnk_presence}
\title{Per-AOI species presence with bcfp species-group expansion}
\usage{
lnk_presence(
  wsg_species_presence,
  aoi,
  groups = list(salmon = c("ch", "cm", "co", "pk", "sk"), ct_dv_rb = c("ct", "dv", "rb"))
)
}
\arguments{
\item{wsg_species_presence}{Data frame or tibble matching the
\code{loaded$wsg_species_presence} shape (per \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}):
\code{watershed_group_code}, then per-species columns (\code{bt}, \code{ch}, ...),
plus optional \code{notes}. Values may be character (\code{"t"}/\code{""}/\code{NA},
the CSV-bundled form) or logical (\code{TRUE}/\code{FALSE}/\code{NA}, the
PostgreSQL form). \code{notes} and \code{watershed_group_code} are excluded
from the species list.}

\item{aoi}{Character. Watershed group code (e.g. \code{"ADMS"}).}

\item{groups}{Named list of character vectors. Each name is a group
tag (e.g. \code{"salmon"}); each value lists species codes that share
group-presence semantics. Default mirrors bcfp:
\itemize{
\item \code{salmon = c("ch", "cm", "co", "pk", "sk")}
\item \code{ct_dv_rb = c("ct", "dv", "rb")}
}

A species in a group is reported present iff \strong{any} group member
is present in the AOI row. Pass \code{list()} to disable expansion.}
}
\value{
A list with:
\itemize{
\item \code{aoi}: echo of input.
\item \code{row}: the raw 1-row tibble for \code{aoi}.
\item \code{present}: character vector of species codes present after
group expansion.
\item \code{absent}: character vector of species codes not present.
\item \code{is_present(sp)}: vectorised function returning \code{TRUE} for
species in \code{present}.
}
}
\description{
Reads a single AOI's row from a \code{wsg_species_presence} tibble and
returns structured presence info: the per-species TRUE/FALSE flags
from the row, expanded by user-supplied species groups so that "any
group member present" promotes the whole group to present. Mirrors
bcfp's \code{wsg_salmon} / \code{wsg_ct_dv_rb} JOIN logic in
\code{load_streams_access.sql} — useful as input to per-species pipeline
loops that should skip absent species.
}
\details{
Coexists with \code{\link[=lnk_pipeline_species]{lnk_pipeline_species()}}, which returns the
intersection of \code{cfg$species} with AOI-present species as a plain
vector. \code{lnk_presence()} is the structured / group-aware sibling.
}
\examples{
\dontrun{
loaded <- lnk_load_overrides(lnk_config("default_extrabreaks"))

# ADMS — BT + salmon-group + ct_dv_rb-group present.
pres <- lnk_presence(loaded$wsg_species_presence, "ADMS")
pres$present                           # bt, ch, co, sk + cm, pk (group-expanded)
pres$absent                            # st, wct, gr, ko
pres$is_present(c("bt", "st", "cm"))   # TRUE FALSE TRUE

# ELKR — salmon all NULL, no group expansion fires.
pres <- lnk_presence(loaded$wsg_species_presence, "ELKR")
pres$is_present("ch")                  # FALSE

# Disable group expansion.
pres <- lnk_presence(loaded$wsg_species_presence, "ADMS",
                     groups = list())
pres$is_present("cm")                  # FALSE (only literal cm column matters)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_access}()},
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_crossings}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_mapping_code}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()}
}
\concept{pipeline}
