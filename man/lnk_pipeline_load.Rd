% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_load.R
\name{lnk_pipeline_load}
\alias{lnk_pipeline_load}
\title{Load Crossings and Apply Crossing-Level Overrides}
\usage{
lnk_pipeline_load(conn, aoi, cfg, schema)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object (localhost fwapg,
typically from \code{\link[=lnk_db_conn]{lnk_db_conn()}}).}

\item{aoi}{Character. Today accepts a watershed group code (e.g.
\code{"BULK"}). Filtering against \code{watershed_group_code} columns in the
CSVs means polygon / ltree AOIs are not yet supported here; those
will come as a follow-up.}

\item{cfg}{An \code{lnk_config} object from \code{\link[=lnk_config]{lnk_config()}}.}

\item{schema}{Character. Working schema name (validated). Must
already exist — call \code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}} first.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Second phase of the habitat classification pipeline. Loads the
anthropogenic crossing table for an AOI and applies the two
crossing-level override types from the config bundle:
}
\details{
\enumerate{
\item \strong{Modelled crossing fixes} — imagery/field corrections where
\code{structure = "NONE"} or \code{structure = "OBS"} force the crossing's
\code{barrier_status} to \code{"PASSABLE"}. These are modelled culverts
that, on inspection, turned out to be open channels or
observation-only points.
\item \strong{PSCIS barrier status overrides} — expert-curated
\code{user_barrier_status} values replace the modelled
\code{barrier_status} for a PSCIS crossing.
}

Falls, user-identified definite barriers, observation exclusions,
and habitat classification CSVs are loaded by
\code{\link[=lnk_pipeline_prepare]{lnk_pipeline_prepare()}} where they are consumed, not here.

Writes to these tables under the caller's working schema:
\itemize{
\item \verb{<schema>.crossings} — base crossings + misc crossings, with
overridden \code{barrier_status} applied
\item \verb{<schema>.crossing_fixes} — modelled fixes for the AOI (only
when the config bundle has fixes matching this AOI)
\item \verb{<schema>.pscis_fixes} — PSCIS status overrides for the AOI
(only when the config bundle has entries matching this AOI)
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg  <- lnk_config("bcfishpass")

schema <- "working_bulk"
lnk_pipeline_setup(conn, schema)
lnk_pipeline_load(conn, aoi = "BULK", cfg = cfg, schema = schema)

# Inspect the result
DBI::dbGetQuery(conn, sprintf(
  "SELECT barrier_status, count(*) FROM \%s.crossings GROUP BY 1", schema))

DBI::dbDisconnect(conn)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()}
}
\concept{pipeline}
