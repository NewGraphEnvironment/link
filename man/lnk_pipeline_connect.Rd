% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_connect.R
\name{lnk_pipeline_connect}
\alias{lnk_pipeline_connect}
\title{Apply Rearing-Spawning and Waterbody Connectivity}
\usage{
lnk_pipeline_connect(
  conn,
  aoi,
  cfg,
  loaded,
  schema,
  species = NULL,
  thresholds_csv = system.file("extdata", "parameters_habitat_thresholds.csv", package =
    "fresh")
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{aoi}{Character. Watershed group code (kept for signature
consistency with the other pipeline phases; not used in this
phase — connectivity operates on the classified table which is
already AOI-scoped).}

\item{cfg}{An \code{lnk_config} object from \code{\link[=lnk_config]{lnk_config()}}.}

\item{loaded}{Named list of tibbles from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}.
Carries \code{parameters_fresh} and \code{wsg_species_presence}.}

\item{schema}{Character. Working schema name (kept for signature
consistency; connectivity reads \code{fresh.streams_habitat} directly).}

\item{species}{Character vector. Species to run connectivity for.
Default derives the same way as \code{\link[=lnk_pipeline_classify]{lnk_pipeline_classify()}}.}

\item{thresholds_csv}{Path to the habitat thresholds CSV. Default
uses the copy shipped with fresh.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Sixth and final phase of the habitat classification pipeline. Runs
the connectivity logic that \code{frs_habitat()} executes internally —
rearing-spawning clustering via \code{\link[fresh:frs_cluster]{fresh::frs_cluster()}} and
connected-waterbody rules via \code{fresh:::.frs_connected_waterbody()}
— configured by per-species flags in \code{loaded$parameters_fresh}:
}
\details{
\itemize{
\item \code{cluster_rearing} — enables three-phase rearing-spawning
clustering for the species
\item \code{cluster_direction}, \code{cluster_bridge_gradient},
\code{cluster_bridge_distance}, \code{cluster_confluence_m} — cluster
parameters
\item \code{cluster_spawning} — enables spawn clustering for rules with
\code{requires_connected: rearing} (e.g. SK spawning adjacent to
rearing lakes)
}

Mutates \code{fresh.streams_habitat} in place, adjusting \code{spawning} /
\code{rearing} booleans per species based on connectivity.

\code{lnk_pipeline_connect} is a thin wrapper over fresh's internal
\code{.frs_run_connectivity} orchestrator. Accessing fresh internals is
an acknowledged fragility — a fresh issue will be filed to export a
stable API for this composition. The wrapper isolates link from the
internal name, so future renames in fresh affect one file here.
}
\examples{
\dontrun{
conn   <- lnk_db_conn()
cfg    <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)
schema <- "working_bulk"

lnk_pipeline_setup(conn, schema)
lnk_pipeline_load(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_prepare(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_break(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_classify(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_connect(conn, "BULK", cfg, loaded, schema)

DBI::dbDisconnect(conn)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()}
}
\concept{pipeline}
