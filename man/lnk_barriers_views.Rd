% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_barriers_views.R
\name{lnk_barriers_views}
\alias{lnk_barriers_views}
\title{Create working-schema views over \verb{<persist_schema>.barriers}}
\usage{
lnk_barriers_views(
  conn,
  schema,
  cfg,
  species = c("BT", "CH", "CM", "CO", "PK", "SK", "ST", "WCT"),
  barriers_table = NULL,
  recreate = FALSE
)
}
\arguments{
\item{conn}{A DBI connection.}

\item{schema}{Working schema name where the views are created.}

\item{cfg}{An \code{lnk_config} object (resolves \code{cfg$pipeline$schema}
for the underlying persist-schema reference).}

\item{species}{Character vector of species codes the views should
cover. Default \code{c("BT","CH","CM","CO","PK","SK","ST","WCT")}.}

\item{barriers_table}{Character or \code{NULL}. Source barriers table the
views read from. Default \code{NULL} → uses \verb{<persist_schema>.barriers}
from \code{cfg} (the original behaviour). Pass a working-schema name
(e.g. \code{paste0(schema, ".barriers")}) to build the views over a
per-WSG working table — used by \code{\link[=lnk_pipeline_run]{lnk_pipeline_run()}}'s mapping_code
phase, which runs BEFORE the per-WSG persist write so persist
barriers may not yet hold current data. Tunnel-free / link-canonical
either way (the underlying table has \code{blocks_species} from
\code{\link[=lnk_barriers_unify]{lnk_barriers_unify()}}).}

\item{recreate}{Logical. \code{FALSE} (default) emits \verb{CREATE OR REPLACE VIEW}
only. \code{TRUE} restores the historical \verb{DROP VIEW IF EXISTS} +
\verb{CREATE OR REPLACE VIEW} pair — needed only when a view's \strong{column
shape} has changed, since \verb{CREATE OR REPLACE} may not rename, retype,
reorder or drop an output column. See Details for why the default
changed (link#250).}
}
\value{
\code{invisible(conn)}. Side effect: creates or replaces two views
per species (\verb{_unified} + \verb{_access}) + three source-typed views in
\code{schema}.
}
\description{
Emits per-species + per-source views in the working schema that
filter the unified province-wide \verb{<persist_schema>.barriers} table
(link#152). Each view exposes the bcfp-shape \verb{<table>_id} column
\code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}} expects (\code{barriers_bt_id}, \code{barriers_dams_id},
...) so the existing \code{barriers_per_sp} + \code{barrier_sources}
consumer code paths run unchanged.
}
\details{
The views point at the province-wide table — cross-WSG dnstr walks
resolve correctly because \code{\link[fresh:frs_network_features]{fresh::frs_network_features()}} walks
FWA topology and reads from the view (which is the unified table).
Fixes the PARS BT 60\% defect (PARS drains through dams in PCEA /
UPCE WSGs) and unblocks any regional run.

Per-species views (two per species \code{bt}, \code{ch}, \code{cm}, \code{co}, \code{pk}, \code{sk},
\code{st}, \code{wct}):
\itemize{
\item \verb{<schema>.barriers_<sp>_unified} — filtered by
\code{'<SP>' = ANY(blocks_species)} (ALL barrier families incl dams).
\verb{_unified} suffix avoids name collision with the per-WSG
\verb{<schema>.barriers_<sp>} tables that \code{.lnk_pipeline_prep_minimal}
builds for the break-time path.
\item \verb{<schema>.barriers_<sp>_access} — the per-species \strong{accessibility}
set that drives \code{accessible} in mapping_code (link#200). Reproduces
bcfp \verb{barriers_<sp>}: NATURAL barriers only
(\verb{barrier_source IN ('GRADIENT','FALLS','SUBSURFACE_FLOW')} — the
gradient-at-species-threshold is already encoded in \code{blocks_species})
MINUS the observation/habitat override (anti-join \code{barrier_overrides}),
∪ all \code{USER_DEFINITE} (override-exempt). Dams/PSCIS/modelled are
excluded — they annotate token2 via the per-source views, never block
access. Same feature shape (\verb{barriers_<sp>_access_id} + ltrees + geom)
so \code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}} / \code{\link[fresh:frs_network_features]{fresh::frs_network_features()}} consume it
unchanged. See \code{RUNBOOK.md} §5.
}

Per-source views (matching the bcfp source-typed tables consumed by
the \code{barrier_sources} arg of \code{lnk_pipeline_access}):
\itemize{
\item \verb{<schema>.barriers_anthropogenic_unified} — \verb{barrier_source IN ('PSCIS','CABD','MODELLED')}.
\item \verb{<schema>.barriers_pscis_unified} — \code{barrier_source = 'PSCIS'}.
\item \verb{<schema>.barriers_dams_unified} — \code{barrier_source = 'CABD'}.
}

(Remediations stay sourced from \verb{<schema>.barriers_remediations}
built by \code{\link[=lnk_barriers_emit]{lnk_barriers_emit()}} — they're consumed by the
\code{remediated_dnstr_ind} path which joins to \verb{<schema>.crossings}
directly, not via the unified barriers table.)

Reruns are safe: each view is emitted with \verb{CREATE OR REPLACE VIEW}, which
is atomic — a concurrent reader sees either the old definition or the new
one, never nothing.

Until link 0.47.3 every statement was preceded by \verb{DROP VIEW IF EXISTS}.
Each statement autocommits, so that pair left a real interval in which the
view \strong{did not exist}, and a concurrent
\code{\link[fresh:frs_network_features]{fresh::frs_network_features()}} walk could fail with
\verb{relation ... does not exist}. The DROP was belt-and-braces from the
function's first commit rather than a fix for anything, and the view column
list has not changed since, so \verb{CREATE OR REPLACE} alone is sufficient.
\code{RUNBOOK.md} §6 records the DROP's cost in the \emph{sequential} case as well:
an orphaned backend holding a lock on \code{barriers_bt_access} blocked every
later \verb{DROP VIEW} indefinitely.

If a future change alters a view's column shape, \verb{CREATE OR REPLACE} will
fail and the error names \code{recreate = TRUE}. That is deliberately not an
automatic fallback — silently reverting to DROP + CREATE would reintroduce
the window at the worst moment, part-way through a parallel recompute
(link#250).

The underlying \verb{<persist_schema>.barriers} table must exist — typically
initialized by \code{\link[=lnk_persist_init]{lnk_persist_init()}} and populated by \code{\link[=lnk_barriers_unify]{lnk_barriers_unify()}}
\itemize{
\item \code{\link[=lnk_pipeline_persist]{lnk_pipeline_persist()}} for all WSGs in the regional scope.
}
}
\examples{
\dontrun{
conn <- lnk_db_conn()
cfg <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)

# ... lnk_persist_init + lnk_pipeline_* + lnk_barriers_unify +
#     lnk_pipeline_persist all already run for all WSGs ...

lnk_barriers_views(conn, schema = "working_pars", cfg = cfg)

lnk_pipeline_access(
  conn,
  segments        = "working_pars.streams",
  aoi             = "PARS",
  barriers_per_sp = setNames(
    paste0("working_pars.barriers_", c("bt","ch","cm","co","pk","sk","st","wct"), "_unified"),
    c("bt","ch","cm","co","pk","sk","st","wct")),
  barrier_sources = list(
    anthropogenic = "working_pars.barriers_anthropogenic_unified",
    pscis         = "working_pars.barriers_pscis_unified",
    dams          = "working_pars.barriers_dams_unified",
    remediations  = "working_pars.barriers_remediations"))
}

}
\seealso{
\code{\link[=lnk_barriers_unify]{lnk_barriers_unify()}}, \code{\link[=lnk_pipeline_access]{lnk_pipeline_access()}},
\code{\link[=lnk_barriers_emit]{lnk_barriers_emit()}}

Other barriers: 
\code{\link{lnk_barriers_emit}()},
\code{\link{lnk_barriers_unify}()}
}
\concept{barriers}
