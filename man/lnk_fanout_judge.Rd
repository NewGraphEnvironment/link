% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_fanout_judge.R
\name{lnk_fanout_judge}
\alias{lnk_fanout_judge}
\title{Judge the outcome of a parallel fan-out}
\usage{
lnk_fanout_judge(
  rc,
  expected,
  label = "fanout",
  allow_empty = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{rc}{A data frame with a \code{job} column and an \code{rc} column, both
character. One row per job that actually reported. An \code{rc} that is \code{NA},
empty, or not a run of digits counts as a \strong{failure}, never as a
neutral: \code{as.integer("abc")} is \code{NA}, \code{NA != 0} is \code{NA}, and \code{which()}
silently drops it.}

\item{expected}{Character vector of job ids that were supposed to run. No
default — see Details.}

\item{label}{Character. Name for the phase, used in the message.}

\item{allow_empty}{Logical. Whether an empty \code{expected} is acceptable.
Default \code{FALSE}.}

\item{quiet}{Logical. Suppress the summary message. Default \code{FALSE}.}
}
\value{
Invisibly, a list with \code{ok}, \code{status}, \code{label}, the counts
(\code{n_expected}, \code{n_ran}, \code{n_ok}, \code{n_failed}, \code{n_missing}, \code{n_unexpected}),
the id vectors (\code{failed}, \code{missing}, \code{unexpected}), the input \code{rc},
plus \code{problems} and a formatted \code{message}.
}
\description{
Decides whether a work-list that was fanned out across N workers actually
completed. Every job is expected to record its own exit status; this reads
that record against the list of jobs that were \emph{supposed} to run and
returns a verdict the caller can act on.
}
\details{
Written for the post-consolidate recompute pool in
\code{data-raw/study_area_run.sh} (link#250), which has no shell test harness —
the predicate lives here because testthat can prove what shell cannot, the
same reasoning that put \code{lnk_preflight_vintage()} and
\code{lnk_preflight_parity()} in R. \code{data-raw/fanout_judge.R} is the
shell-callable wrapper.
}
\section{Why it takes names, not a count}{

A count answers "how many finished" and cannot answer "which one didn't".
Passing the expected job ids lets the verdict name the jobs that never
reported, and lets it notice a job that reported but was never asked for —
which is a harness bug, not a work failure, and needs a different fix.

\code{expected} deliberately has \strong{no default}. Judging a result table on its
own terms produces an affirmative claim of success about jobs that never
reported at all — the failure mode this function exists to prevent. Same
doctrine as \code{n_expected} in \code{\link[=lnk_preflight_parity]{lnk_preflight_parity()}}.
}

\section{Statuses}{

Evaluated in order, so the first that applies wins:

\describe{
\item{\code{none_expected}}{\code{expected} is empty. A fan-out over nothing exits
0 exactly like a successful one, so this is a failure unless the caller
passes \code{allow_empty = TRUE}.}
\item{\code{none_ran}}{Nothing reported. Distinct from \code{all_failed}: no job
got far enough to record anything, which points at the harness rather
than at the work.}
\item{\code{all_failed}}{Every job that reported failed, and none succeeded.
Reported separately from how many are missing — both counts are always
available.}
\item{\code{ok}}{Every expected job reported, every status was zero, and there
were no duplicate or unexpected ids.}
\item{\code{partial}}{Anything else — some ran, some did not, or some failed.}
}
}

\examples{
ran <- data.frame(job = c("BULK", "PARS", "ADMS"),
                  rc  = c("0", "0", "0"),
                  stringsAsFactors = FALSE)
res <- lnk_fanout_judge(ran, expected = c("BULK", "PARS", "ADMS"))
res$status

# A job that failed and a job that never reported are different problems.
partial <- data.frame(job = c("BULK", "PARS"), rc = c("0", "1"),
                      stringsAsFactors = FALSE)
bad <- lnk_fanout_judge(partial, expected = c("BULK", "PARS", "ADMS"),
                        label = "recompute", quiet = TRUE)
bad$failed
bad$missing

}
\seealso{
\code{\link[=lnk_preflight_parity]{lnk_preflight_parity()}}

Other preflight: 
\code{\link{lnk_preflight_fresh}()},
\code{\link{lnk_preflight_parity}()},
\code{\link{lnk_preflight_stamp}()},
\code{\link{lnk_preflight_vintage}()}
}
\concept{preflight}
