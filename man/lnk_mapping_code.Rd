% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_mapping_code.R
\name{lnk_mapping_code}
\alias{lnk_mapping_code}
\title{Build per-segment per-species mapping_code tokens from schema tables}
\usage{
lnk_mapping_code(
  conn,
  table_access,
  table_habitat,
  table_streams,
  aoi,
  table_to = NULL,
  presence = NULL,
  species_resident = c("bt", "wct"),
  species_anadromous = c("ch", "cm", "co", "pk", "sk", "st"),
  species_spawn_only = c("cm", "pk")
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} to the local pipeline DB.}

\item{table_access}{Character. Schema-qualified name of the
\code{streams_access} table (e.g. \code{"working_pars.streams_access"} or
\code{"fresh_default.streams_access"}).}

\item{table_habitat}{Character. Schema-qualified name of a long-form
habitat source — either the working-schema \code{streams_habitat} table
or the persist \code{streams_habitat_long_vw} view. Must have columns
\code{id_segment}, \code{watershed_group_code}, \code{species_code}, \code{spawning},
\code{rearing}.}

\item{table_streams}{Character. Schema-qualified name of the
\code{streams} table, queried for \code{id_segment} + \code{feature_code}.}

\item{aoi}{Character. Watershed group code (e.g. \code{"PARS"}) — filters
all input queries to one WSG.}

\item{table_to}{Character or \code{NULL}. Optional schema-qualified
destination table for the result. When non-NULL,
\code{lnk_pipeline_mapping_code()} writes the tibble via
\code{dbWriteTable(overwrite = TRUE)}. Default \code{NULL} — returns-only.}

\item{presence}{Named logical vector or \code{NULL}. Per-species presence
flag for \code{aoi}. When \code{NULL} the function derives presence from
the data: a species is present iff it has at least one habitat row
with \code{spawning = TRUE} or \code{rearing = TRUE}. Pass explicit values
to override (e.g. force-include a species for QGIS symbology even
when no segments are accessible).}

\item{species_resident}{Character. Species using the resident flavor
of \code{mapping_code_barrier}. Default \code{c("bt", "wct")}. Pass-through
to \code{\link[=lnk_pipeline_mapping_code]{lnk_pipeline_mapping_code()}}'s \code{resident_species} arg.}

\item{species_anadromous}{Character. Species using the anadromous
flavor. Default \code{c("ch", "cm", "co", "pk", "sk", "st")}.
Pass-through to \code{lnk_pipeline_mapping_code()}'s
\code{anadromous_species} arg.}

\item{species_spawn_only}{Character. Species without rearing
semantics. Default \code{c("cm", "pk")}. Pass-through to
\code{lnk_pipeline_mapping_code()}'s \code{spawn_only_species} arg.}
}
\value{
Invisibly, the per-segment per-species mapping_code tibble
keyed by \code{id_segment} with one \verb{mapping_code_<sp>} text column per
species in \code{union(species_resident, species_anadromous)}.
}
\description{
Schema-aware portable wrapper around \code{\link[=lnk_pipeline_mapping_code]{lnk_pipeline_mapping_code()}}.
Queries \code{table_access}, \code{table_habitat} (long form), and
\code{table_streams.feature_code}, assembles the inputs (pivot habitat
long → wide, build feature_code lookup), calls
\code{\link[=lnk_pipeline_mapping_code]{lnk_pipeline_mapping_code()}} for the per-segment token compute, and
optionally writes the result to \code{table_to}.
}
\details{
Decouples the mapping_code build from any specific schema layout.
Caller passes explicit table names — the function works against
working-schema tables (mid-pipeline) or persist-schema tables
(ad-hoc rebuild) without modification. The companion view
\verb{<persist_schema>.streams_habitat_long_vw} (created by
\code{\link[=lnk_persist_init]{lnk_persist_init()}}) presents the per-species split as a long-form
shape so \code{table_habitat} can point at either layout.

This function replaces the inline assembly previously buried inside
\code{lnk_compare_wsg()}. The compare wrapper now goes through this
function via \code{lnk_pipeline_run(..., mapping_code = TRUE)}. Operators
can also call this directly against persist schema with the tunnel
down — the build is tunnel-independent (the diff vs reference is
separate, see \code{\link[=lnk_compare_mapping_code]{lnk_compare_mapping_code()}}).

Tracks link#187 (tunnel decouple + portable build).
}
\examples{
\dontrun{
conn <- DBI::dbConnect(
  RPostgres::Postgres(),
  host = "localhost", port = 5432, dbname = "fwapg",
  user = "postgres", password = "postgres")

# Working-schema build during a pipeline run:
lnk_mapping_code(
  conn,
  table_access  = "working_pars.streams_access",
  table_habitat = "working_pars.streams_habitat",
  table_streams = "working_pars.streams",
  aoi           = "PARS",
  table_to      = "working_pars.streams_mapping_code")

# Ad-hoc rebuild against persist (tunnel-free) for QGIS symbology:
lnk_mapping_code(
  conn,
  table_access  = "fresh_default.streams_access",
  table_habitat = "fresh_default.streams_habitat_long_vw",
  table_streams = "fresh_default.streams",
  aoi           = "PARS",
  table_to      = "fresh_default.streams_mapping_code")
}

}
\seealso{
Other compare: 
\code{\link{lnk_access}()},
\code{\link{lnk_compare_mapping_code}()},
\code{\link{lnk_compare_rollup}()},
\code{\link{lnk_compare_wsg}()},
\code{\link{lnk_parity_annotate}()},
\code{\link{lnk_rollup_wsg}()}
}
\concept{compare}
