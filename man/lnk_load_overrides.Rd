% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_load_overrides.R
\name{lnk_load_overrides}
\alias{lnk_load_overrides}
\title{Materialize the Tabular Data Files Declared in a Config Bundle}
\usage{
lnk_load_overrides(cfg)
}
\arguments{
\item{cfg}{An \code{lnk_config} object returned by \code{\link[=lnk_config]{lnk_config()}}, or a
character (config name or path) — for ergonomic call.}
}
\value{
Named list of tibbles. Order matches \code{cfg$files}.
}
\description{
Walks \code{cfg$files} and returns a named list of tibbles, one per
entry. Entries with a \code{canonical_schema} field dispatch through
\code{\link[crate:crt_ingest]{crate::crt_ingest()}} (which handles canonicalization across
upstream variants). Entries without \code{canonical_schema} fall through
to a local read dispatched on the path's extension (\code{.csv} today;
more formats can be added without schema changes).
}
\details{
Returned list keys match the entry keys in \code{cfg$files} exactly
(filename-stem convention — see \verb{inst/extdata/configs/<name>/config.yaml}).
}
\examples{
cfg <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)
names(loaded)
head(loaded$user_habitat_classification)
head(loaded$parameters_fresh)

\dontrun{
# Same call shape with a project-experimental config that extends default
loaded_proj <- lnk_load_overrides("/path/to/project/config")
}
}
