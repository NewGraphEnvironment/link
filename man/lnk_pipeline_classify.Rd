% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_classify.R
\name{lnk_pipeline_classify}
\alias{lnk_pipeline_classify}
\title{Classify Stream Segments into Habitat per Species}
\usage{
lnk_pipeline_classify(
  conn,
  aoi,
  cfg,
  loaded,
  schema,
  species = NULL,
  thresholds_csv = system.file("extdata", "parameters_habitat_thresholds.csv", package =
    "fresh")
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{aoi}{Character. Watershed group code (today; extends to other
spatial filters later).}

\item{cfg}{An \code{lnk_config} object from \code{\link[=lnk_config]{lnk_config()}}.}

\item{loaded}{Named list of tibbles from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}.
Carries \code{parameters_fresh}, \code{user_habitat_classification}, and
\code{wsg_species_presence}.}

\item{schema}{Character. Working schema name.}

\item{species}{Character vector. Species codes to classify. Default
derives from \code{loaded$parameters_fresh$species_code} intersected
with the species present in the AOI (via
\code{loaded$wsg_species_presence}).}

\item{thresholds_csv}{Path to the habitat thresholds CSV. Default
uses the copy shipped with fresh.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Fifth phase of the habitat classification pipeline. Builds the
access-gating break table consumed by classification, then calls
\code{\link[fresh:frs_habitat_classify]{fresh::frs_habitat_classify()}} with the rules YAML, thresholds,
per-species parameters, and barrier overrides from the config
bundle.
}
\details{
The access-gating break table (\code{fresh.streams_breaks}) is assembled
from the FULL gradient barrier set (not the minimal one used for
segmentation), falls, user-identified definite barriers, and
crossings with their AOI-filtered ltree values attached. Filtering
to the AOI keeps the O(segments × breaks) access-gating join
tractable.

Writes to:
\itemize{
\item \code{fresh.streams_breaks} — access-gating breaks
\item \code{fresh.streams_habitat} — per-species classification output
(written by \code{frs_habitat_classify})
}
}
\examples{
\dontrun{
conn   <- lnk_db_conn()
cfg    <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)
schema <- "working_bulk"

lnk_pipeline_setup(conn, schema)
lnk_pipeline_load(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_prepare(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_break(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_classify(conn, "BULK", cfg, loaded, schema)

DBI::dbDisconnect(conn)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_prepare}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()}
}
\concept{pipeline}
