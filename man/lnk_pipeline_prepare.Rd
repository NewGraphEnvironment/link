% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lnk_pipeline_prepare.R
\name{lnk_pipeline_prepare}
\alias{lnk_pipeline_prepare}
\title{Prepare the Network and Barrier Inputs for a Pipeline Run}
\usage{
lnk_pipeline_prepare(
  conn,
  aoi,
  cfg,
  loaded,
  schema,
  observations = "bcfishobs.observations",
  conn_tunnel = NULL
)
}
\arguments{
\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object.}

\item{aoi}{Character. Watershed group code today; extends to ltree
filters / sf polygons later (same AOI abstraction fresh uses).}

\item{cfg}{An \code{lnk_config} object from \code{\link[=lnk_config]{lnk_config()}}.}

\item{loaded}{Named list of tibbles from \code{\link[=lnk_load_overrides]{lnk_load_overrides()}}.
Carries \code{user_barriers_definite}, \code{user_barriers_definite_control},
\code{user_habitat_classification}, and \code{parameters_fresh}.}

\item{schema}{Character. Working schema name (must already exist —
call \code{\link[=lnk_pipeline_setup]{lnk_pipeline_setup()}} first).}

\item{observations}{Character. Schema-qualified observations table
used for building barrier overrides. Default
\code{"bcfishobs.observations"} — matches bcfishpass's reference data
on both M4 and M1 fwapg instances (see \code{rtj/docs/distributed-fwapg.md}).}

\item{conn_tunnel}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection} object pointed at
\code{db_newgraph} (the tunnel-DB carrying bcfp's pre-built tables).
Optional. When supplied, \verb{<schema>.dams} is populated from
\code{bcfishpass.dams} filtered to the AOI — parallel reporting layer
for downstream consumers, NOT consumed by habitat classification.
When \code{NULL}, any existing \verb{<schema>.dams} is dropped and the dams
step is a no-op.}
}
\value{
\code{conn} invisibly, for pipe chaining.
}
\description{
Third phase of the habitat classification pipeline. Loads the
evidence and network data that downstream phases (\code{break},
\code{classify}, \code{connect}) consume:
}
\details{
\itemize{
\item Falls (from the \code{fresh} package), user-identified definite
barriers, user barriers-definite control table, and expert
habitat confirmation CSVs from the config bundle
\item Gradient barriers detected on the raw FWA network via
\code{\link[fresh:frs_break_find]{fresh::frs_break_find()}}, pruned against the control table,
enriched with \code{wscode_ltree} and \code{localcode_ltree} for
\code{fwa_upstream()} joins
\item A natural-barriers table (gradient + falls) used by
\code{lnk_barrier_overrides()} to compute the per-species skip list.
User-definite barriers are intentionally excluded here and
consumed by later phases directly — bcfishpass parity.
\item Per-model barrier tables reduced to the minimal downstream-most
set via \code{\link[fresh:frs_barriers_minimal]{fresh::frs_barriers_minimal()}}, then unioned into
\code{gradient_barriers_minimal} for segmentation
\item Base stream segments (\code{fresh.streams}) loaded from FWA with
channel width, stream order parent, GENERATED gradient / measures
/ length columns, and a unique \code{id_segment}
}

Writes to (under the caller's working schema unless noted):
\itemize{
\item \verb{<schema>.falls}, \verb{<schema>.barriers_definite},
\verb{<schema>.barriers_definite_control},
\verb{<schema>.user_habitat_classification}
\item \verb{<schema>.gradient_barriers_raw} (with ltree)
\item \verb{<schema>.natural_barriers} (gradient + falls + opt-in subsurfaceflow)
\item \verb{<schema>.barriers_subsurfaceflow} (only when subsurfaceflow opted in
via \code{cfg$pipeline$break_order})
\item \verb{<schema>.barrier_overrides}
\item \verb{<schema>.barriers_<model>} + \verb{<schema>.barriers_<model>_min}
per-model pre/post minimal reduction
\item \verb{<schema>.gradient_barriers_minimal} (union of minimal positions)
\item \code{fresh.streams} (base segments — not namespaced by AOI; fresh
owns its output schema)
\item \verb{<schema>.dams} (only when \code{conn_tunnel} is supplied) — pulled
from \code{bcfishpass.dams} filtered to AOI. Parallel reporting layer;
NOT consumed by habitat classification.
}
}
\examples{
\dontrun{
conn   <- lnk_db_conn()
cfg    <- lnk_config("bcfishpass")
loaded <- lnk_load_overrides(cfg)
schema <- "working_bulk"

lnk_pipeline_setup(conn, schema)
lnk_pipeline_load(conn, "BULK", cfg, loaded, schema)
lnk_pipeline_prepare(conn, "BULK", cfg, loaded, schema)

DBI::dbGetQuery(conn, sprintf(
  "SELECT count(*) FROM \%s.gradient_barriers_minimal", schema))

DBI::dbDisconnect(conn)
}
}
\seealso{
Other pipeline: 
\code{\link{lnk_pipeline_break}()},
\code{\link{lnk_pipeline_classify}()},
\code{\link{lnk_pipeline_connect}()},
\code{\link{lnk_pipeline_load}()},
\code{\link{lnk_pipeline_setup}()},
\code{\link{lnk_pipeline_species}()}
}
\concept{pipeline}
